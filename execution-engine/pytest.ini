[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
addopts = -v -ra -q --cov=src
markers =
    asyncio: mark test as an async test
    gpu: mark test as requiring GPU
log_cli = true
log_cli_level = INFO